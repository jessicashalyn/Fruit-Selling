version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: fruitshop
      POSTGRES_USER: fruitshop_user
      POSTGRES_PASSWORD: secretpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
